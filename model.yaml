apiVersion: apps/v1
kind: Deployment
metadata:
  name: bad-model-fake-weights
  labels:
    app: bad-model-fake-weights
spec:
  ...
  template:
  ...
    spec:
      initContainers:
        - name: weights-downloader
        ...
        - name: weights-verifier
        ...
      containers:
        - name: bad-model-fake-weights
          image: olegdayo/example-service:v2.0.5
          ports:
            - containerPort: 8000
          resources:
          ...
      volumes:
      ...
