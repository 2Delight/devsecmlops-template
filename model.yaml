apiVersion: apps/v1
kind: Deployment
metadata:
  name: bad-model-unsigned-image
  labels:
    app: bad-model-unsigned-image
spec:
  ...
  template:
  ...
    spec:
      initContainers:
        - name: weights-downloader
        ...
        - name: weights-verifier
        ...
      containers:
        - name: bad-model-unsigned-image
          image: olegdayo/example-service:v2.0.1
          ports:
            - containerPort: 8000
          resources:
          ...
      volumes:
      ...
